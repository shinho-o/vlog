# 로컬 파이프라인 전용 (Render 배포에선 설치 X)
#   pip install -r requirements.txt -r requirements-local.txt

-r requirements.txt

yt-dlp>=2024.12.13
faster-whisper>=1.0.0
scenedetect[opencv]>=0.6.4
librosa>=0.10.0
Pillow>=10.0.0
